"use Strict"; // this is used to tell the computer that all the code in this file will be writen as modern js code.

alert("hello") // this will not work in node

let string = "Zahhad"
let number = 21 // uper limit of what a number could hold in memory is 2^53. For something bigger use "bigint dataType" 
let bolean = false
let null = null // "null" is used when there is intentional absence of object value
let undefined // "undefined" == value is non-existent



/*
-> Code Readability >> Efficient Code
 */